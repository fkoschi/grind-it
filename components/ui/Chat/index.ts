import { ChatAvatar } from "./ChatAvatar";
import { ChatInput } from "./ChatInput";
import { ChatMessage } from "./ChatMessage";

export const Chat = {
  Avatar: ChatAvatar,
  Input: ChatInput,
  Message: ChatMessage,
};
